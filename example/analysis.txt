package StreamStatementExample {
	
	stream integerStreamz {
		int element;
	}
	
	int az = 1;
	int bz = 2;
	int cz = 3;
	
	integerStream r;
	integerStream s;
	integerStream t;
	integerStream u;
	
	
	r = avg(s.element, s[now]);
	t = std(r.element, r[last 20 elements]);
	u = count(t.element, u[last 10 min]);
}