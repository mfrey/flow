package StreamStatementExample {
	
	stream integerStreamz {
		int element;
	}
	
	int az = 1;
	int bz = 2;
	int cz = 3;
	
	integerStream r;
	integerStream s;
	integerStream t;
	integerStream u;
	integerStream v;
	
	
	r = add(s.element, 5, s[now]);
	t = sub(r.element, 42.3, s[now]);
	u = mult(t.element, r.element, r[now]);
	v = div(u.element, 9, u[last 10 elements]);
}