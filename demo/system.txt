package system {

	stream integerStream {
		int element;
		float anotherElement;
	}
	
	integerStream a;
	integerStream b;

	integerStream cacheIn;
	integerStream cacheOut;
	integerStream logStream;
	
	cacheIn = in("http://www.anothertest.de", "localhost", 9595);
	a, b = split(cacheIn);
	cacheOut = out(a, "http://www.anothertest.de", "localhost", 9596);
	logStream = log("generated3.txt", "aal", b);
}

