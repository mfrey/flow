package arithmetic {

	stream integerStream {
		int element;
		float anotherElement;
	}
	
	int newElement = 2;
	int o = 42;
	
	integerStream a;
	integerStream b;
	integerStream c;
	integerStream d;
	integerStream e;
	integerStream f;
	integerStream g;
	integerStream h;

	a = in("http://www.test.de", "localhost", 9595);
	// Operation: d = a.element + 2
	d = add(a.element, 2);
	// Operation: e = d.element - o
	e = sub(d.element, o);
	// Operation: f = e.element * e.anotherElement
	f = mult(e.element, e.anotherElement);
	// Operation: g = f.element / 3
	g = div(f.element, 3);
	// Operation: h = g.element % 2
	h = mod(g.element, 2);
}

